'use strict';

// Oefening 08
let x = parseInt(prompt('Geef een getal X op:')),
  y = parseInt(prompt('Geef een getal Y op:'));
let total = (x + y) ** 2;

console.log(`(${x} + ${y}) ^ 2 = ${total}`);
